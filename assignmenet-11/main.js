/*11. Names: Store the names of a few of your friends in a array called names.
Print each person’s name by accessing each element in the list, one at a time.*/
var friends_names = ["Rabia", "saima", "Ruby", "Nayar", "Aliya", "Tahira", "shahina"]; //store names friend
friends_names.forEach(function (fname) {
    console.log(fname);
}); // print/ Display each name on each line.
